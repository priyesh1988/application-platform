apiVersion: v1
kind: Service
metadata:
  name: {{ include "application-platform.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "application-platform.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
